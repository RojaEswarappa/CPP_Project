git clone  git url
git init
git remote add origin git url
git checkout -b branch name
.. do some modification
..to check status git status
git add --all
git commit -m "message"
git pull -u origin branch name ->-u for upstream
then go to github now



for checkout from the branch and do modify and then push it

git init
git remote add origin url
git remote -v        //to check origin is linked or not
git fetch origin    //it will show the branch 
git checkout -b  branchname origin/branchname        
do modification
git status
git add file name or git add --all
git commit -m "message"
git push origin branchname
